import React, { Fragment } from "react";
import Register from "../../components/register";

const index = () => {
  return (
    <Fragment>
      <Register />
    </Fragment>
  );
};

export default index;
