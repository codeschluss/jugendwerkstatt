import React from "react";
import _ProfileSettings from "../../components/Profile/settings";

const ProfileSettings = () => {
  // eslint-disable-next-line react/jsx-pascal-case
  return <_ProfileSettings />;
};

export default ProfileSettings;
