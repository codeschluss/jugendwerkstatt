query GetChat($entity: ChatEntityInput) {
  getChat(entity: $entity) {
    id
    name
    admin
    participants {
      id
      user {
        fullname
        id
        profilePicture {
          id
        }
      }
    }
  }
}
