query GetUsers($params: FilterSortPaginateInput) {
  getUsers(params: $params) {
    result {
      id
      fullname
      phone
      email
      profilePicture {
        id
        mimeType
        base64
      }
    }
  }
}
