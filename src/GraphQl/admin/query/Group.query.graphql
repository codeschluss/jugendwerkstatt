query GetGroups {
  groups: getGroups {
    result {
      ...GroupField
      users: {
        id
      }
    }
  }
}

query GetGroup($id: String) {
  group: getGroup(entity: { id: $id }) {
    ...GroupField
  }
}
